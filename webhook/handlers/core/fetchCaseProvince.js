const handler = (interaction) => {
    return new Promise((resolve, reject) => {
    
  
      // Fetch Case by Province
      let CaseProvince= ""
      interaction.response = ""
  
      // Indicate the action has been performed successfully
      resolve()
    })
  }
  

module.exports = handler